# Component

```
    config          这里主要是借鉴douyu框架得config解析
    email           邮件
    gorm            orm gorm v2
    log             日志
    mongo           mongoDB
    oss             云对象存储
    redis           redis
    pkg
      -xcast        spf13/cast不足的函数封装
      -xcolor       颜色输出
      -xfile        文件操作封装
      -xflag        flag封装
      -xgp          协程池子
      -xmap         map操作封装
      -xsignals     信号     
      -xtransform   数组转换封装   
      -xvalidator   验证
```

方便开发，自用

很多东西都是来自开源的项目。